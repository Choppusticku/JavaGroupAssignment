package classes;

public class Admin extends User {

    public Admin(String userId, String username, String password, UserType userType) {
        super(userId, username, password, userType);
    }
    
    
}